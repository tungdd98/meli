# Meli Project Initialization — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Khởi tạo Nx Monorepo cho SaaS web app với React, MUI, TanStack Router, Zustand, React Query, cùng ESLint, Prettier, commitlint, Husky.

**Architecture:** Nx monorepo với 1 React app (`web`) và 4 shared libs (`ui`, `utils`, `hooks`, `api`). App dùng TanStack Router (file-based), Zustand cho client state, React Query cho server state, MUI cho UI. Linting/formatting enforce qua git hooks.

**Tech Stack:** React 19, TypeScript, Vite 6, Nx, MUI v6, TanStack Router, TanStack React Query v5, Zustand, ESLint 9, Prettier, commitlint, Husky, pnpm

---

## File Map

### Root config files
- `nx.json` — Nx workspace config (generated by Nx, modified)
- `tsconfig.base.json` — shared TypeScript config (generated by Nx)
- `package.json` — root package.json (generated by Nx, modified)
- `pnpm-workspace.yaml` — pnpm workspaces (generated by Nx)
- `.prettierrc` — Prettier config (created)
- `commitlint.config.ts` — commitlint config (created)
- `.husky/pre-commit` — lint-staged hook (created)
- `.husky/commit-msg` — commitlint hook (created)
- `.npmrc` — force exact versions (created)

### App: apps/web
- `apps/web/src/main.tsx` — entry point (modified)
- `apps/web/src/app/App.tsx` — root component (modified)
- `apps/web/src/app/providers.tsx` — all providers combined (created)
- `apps/web/src/app/router.tsx` — TanStack Router config (created)
- `apps/web/src/routes/__root.tsx` — root layout route (created)
- `apps/web/src/routes/index.tsx` — home page route (created)
- `apps/web/src/routes/about.tsx` — example route (created)
- `apps/web/src/stores/app.store.ts` — example Zustand store (created)
- `apps/web/src/styles/theme.ts` — MUI theme (created)
- `apps/web/vite.config.ts` — Vite config (modified)

### Libs
- `libs/ui/src/index.ts` — barrel export (generated by Nx)
- `libs/utils/src/index.ts` — barrel export (generated by Nx)
- `libs/hooks/src/index.ts` — barrel export (generated by Nx)
- `libs/api/src/index.ts` — barrel export (generated by Nx)

---

### Task 1: Scaffold Nx Workspace

**Files:**
- Create: toàn bộ cấu trúc Nx workspace (nx.json, tsconfig.base.json, package.json, apps/web/...)

- [ ] **Step 1: Tạo Nx workspace trong thư mục tạm**

```bash
cd /tmp && npx create-nx-workspace@latest meli-tmp \
  --preset=react-monorepo \
  --appName=web \
  --bundler=vite \
  --style=css \
  --e2eTestRunner=none \
  --packageManager=pnpm \
  --nxCloud=skip \
  --skipGit
```

Expected: Nx tạo workspace tại `/tmp/meli-tmp/` với app `web`.

- [ ] **Step 2: Copy nội dung vào project directory**

```bash
cp -r /tmp/meli-tmp/* /tmp/meli-tmp/.[!.]* /Users/mac/Desktop/meli/
rm -rf /tmp/meli-tmp
```

Expected: Thư mục `meli/` có đầy đủ cấu trúc Nx (nx.json, apps/web/, package.json...).

- [ ] **Step 3: Tạo .npmrc để force exact versions**

Tạo file `.npmrc` tại root:

```
save-exact=true
```

- [ ] **Step 4: Install dependencies**

```bash
cd /Users/mac/Desktop/meli && pnpm install
```

Expected: `node_modules/` được tạo, không có lỗi.

- [ ] **Step 5: Verify app chạy được**

```bash
cd /Users/mac/Desktop/meli && npx nx serve web
```

Expected: Vite dev server chạy, truy cập `http://localhost:4200` thấy trang welcome Nx.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: scaffold nx workspace with react-monorepo preset"
```

---

### Task 2: Tạo 4 Shared Libraries

**Files:**
- Create: `libs/ui/`, `libs/utils/`, `libs/hooks/`, `libs/api/` (qua Nx generator)

- [ ] **Step 1: Generate lib `ui`**

```bash
npx nx generate @nx/react:library ui --directory=libs/ui --bundler=vite --unitTestRunner=none --skipFormat
```

Expected: `libs/ui/src/index.ts` được tạo, tsconfig path alias `@meli/ui` được thêm vào `tsconfig.base.json`.

- [ ] **Step 2: Generate lib `utils`**

```bash
npx nx generate @nx/js:library utils --directory=libs/utils --bundler=vite --unitTestRunner=none --skipFormat
```

Expected: `libs/utils/src/index.ts` được tạo, path alias `@meli/utils` đã có.

- [ ] **Step 3: Generate lib `hooks`**

```bash
npx nx generate @nx/react:library hooks --directory=libs/hooks --bundler=vite --unitTestRunner=none --skipFormat
```

Expected: `libs/hooks/src/index.ts` được tạo, path alias `@meli/hooks` đã có.

- [ ] **Step 4: Generate lib `api`**

```bash
npx nx generate @nx/js:library api --directory=libs/api --bundler=vite --unitTestRunner=none --skipFormat
```

Expected: `libs/api/src/index.ts` được tạo, path alias `@meli/api` đã có.

- [ ] **Step 5: Verify path aliases trong tsconfig.base.json**

```bash
cat tsconfig.base.json | grep -A 1 "@meli/"
```

Expected: 4 aliases `@meli/ui`, `@meli/utils`, `@meli/hooks`, `@meli/api` đều có.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: add shared libraries (ui, utils, hooks, api)"
```

---

### Task 3: Install & Configure MUI

**Files:**
- Modify: `package.json` (thêm dependencies)
- Create: `apps/web/src/styles/theme.ts`

- [ ] **Step 1: Install MUI packages**

```bash
pnpm add -E @mui/material @mui/icons-material @emotion/react @emotion/styled
```

Expected: 4 packages được thêm vào `package.json` với exact version (không có `^`).

- [ ] **Step 2: Tạo MUI theme**

Tạo file `apps/web/src/styles/theme.ts`:

```typescript
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
  typography: {
    fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
  },
});

export default theme;
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: install and configure MUI with custom theme"
```

---

### Task 4: Install & Configure TanStack Router

**Files:**
- Modify: `package.json`, `apps/web/vite.config.ts`
- Create: `apps/web/src/app/router.tsx`, `apps/web/src/routes/__root.tsx`, `apps/web/src/routes/index.tsx`, `apps/web/src/routes/about.tsx`
- Delete: default Nx routing files (nếu có)

- [ ] **Step 1: Remove react-router nếu có**

```bash
pnpm remove react-router react-router-dom 2>/dev/null; echo "done"
```

- [ ] **Step 2: Install TanStack Router**

```bash
pnpm add -E @tanstack/react-router @tanstack/react-router-with-query
pnpm add -E -D @tanstack/router-plugin @tanstack/router-devtools
```

Expected: Packages thêm vào `package.json` với exact version.

- [ ] **Step 3: Thêm TanStack Router Vite plugin**

Sửa `apps/web/vite.config.ts`:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { TanStackRouterVite } from '@tanstack/router-plugin/vite';
import { nxViteTsPaths } from '@nx/vite/plugins/nx-tsconfig-paths.plugin';

export default defineConfig({
  root: __dirname,
  cacheDir: '../../node_modules/.vite/apps/web',
  server: {
    port: 4200,
    host: 'localhost',
  },
  preview: {
    port: 4300,
    host: 'localhost',
  },
  plugins: [
    TanStackRouterVite({
      routesDirectory: './src/routes',
      generatedRouteTree: './src/app/routeTree.gen.ts',
    }),
    react(),
    nxViteTsPaths(),
  ],
  build: {
    outDir: '../../dist/apps/web',
    emptyOutDir: true,
    reportCompressedSize: true,
  },
});
```

- [ ] **Step 4: Tạo root route**

Tạo `apps/web/src/routes/__root.tsx`:

```tsx
import { createRootRoute, Outlet } from '@tanstack/react-router';
import { TanStackRouterDevtools } from '@tanstack/router-devtools';
import Box from '@mui/material/Box';

export const Route = createRootRoute({
  component: RootLayout,
});

function RootLayout() {
  return (
    <Box>
      <Outlet />
      <TanStackRouterDevtools position="bottom-right" />
    </Box>
  );
}
```

- [ ] **Step 5: Tạo index route**

Tạo `apps/web/src/routes/index.tsx`:

```tsx
import { createFileRoute } from '@tanstack/react-router';
import Typography from '@mui/material/Typography';
import Container from '@mui/material/Container';

export const Route = createFileRoute('/')({
  component: HomePage,
});

function HomePage() {
  return (
    <Container maxWidth="sm" sx={{ mt: 4 }}>
      <Typography variant="h3" gutterBottom>
        Meli
      </Typography>
      <Typography variant="body1">Welcome to Meli.</Typography>
    </Container>
  );
}
```

- [ ] **Step 6: Tạo about route**

Tạo `apps/web/src/routes/about.tsx`:

```tsx
import { createFileRoute } from '@tanstack/react-router';
import Typography from '@mui/material/Typography';
import Container from '@mui/material/Container';

export const Route = createFileRoute('/about')({
  component: AboutPage,
});

function AboutPage() {
  return (
    <Container maxWidth="sm" sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>
        About
      </Typography>
      <Typography variant="body1">About Meli.</Typography>
    </Container>
  );
}
```

- [ ] **Step 7: Tạo router config**

Tạo `apps/web/src/app/router.tsx`:

```tsx
import { createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';

export const router = createRouter({ routeTree });

declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
```

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat: replace react-router with TanStack Router (file-based)"
```

---

### Task 5: Install & Configure Zustand + React Query

**Files:**
- Modify: `package.json`
- Create: `apps/web/src/stores/app.store.ts`

- [ ] **Step 1: Install Zustand và React Query**

```bash
pnpm add -E zustand @tanstack/react-query
pnpm add -E -D @tanstack/react-query-devtools
```

Expected: Packages thêm vào `package.json` với exact version.

- [ ] **Step 2: Tạo example Zustand store**

Tạo `apps/web/src/stores/app.store.ts`:

```typescript
import { create } from 'zustand';

interface AppState {
  sidebarOpen: boolean;
  toggleSidebar: () => void;
}

export const useAppStore = create<AppState>((set) => ({
  sidebarOpen: false,
  toggleSidebar: () => set((state) => ({ sidebarOpen: !state.sidebarOpen })),
}));
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: install and configure Zustand and React Query"
```

---

### Task 6: Wire Up Providers & App Entry Point

**Files:**
- Create: `apps/web/src/app/providers.tsx`
- Modify: `apps/web/src/app/App.tsx`, `apps/web/src/main.tsx`

- [ ] **Step 1: Tạo providers**

Tạo `apps/web/src/app/providers.tsx`:

```tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';
import { RouterProvider } from '@tanstack/react-router';
import { ThemeProvider } from '@mui/material/styles';
import CssBaseline from '@mui/material/CssBaseline';
import theme from '../styles/theme';
import { router } from './router';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,
      retry: 1,
    },
  },
});

export function Providers() {
  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />
      <QueryClientProvider client={queryClient}>
        <RouterProvider router={router} />
        <ReactQueryDevtools initialIsOpen={false} />
      </QueryClientProvider>
    </ThemeProvider>
  );
}
```

- [ ] **Step 2: Cập nhật App.tsx**

Thay nội dung `apps/web/src/app/App.tsx`:

```tsx
import { Providers } from './providers';

export function App() {
  return <Providers />;
}

export default App;
```

- [ ] **Step 3: Verify main.tsx render App**

Đảm bảo `apps/web/src/main.tsx` có nội dung:

```tsx
import { StrictMode } from 'react';
import * as ReactDOM from 'react-dom/client';
import App from './app/App';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement,
);

root.render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 4: Xóa file thừa từ scaffold**

Xóa các file mà Nx tạo sẵn nhưng không cần:

```bash
rm -f apps/web/src/app/nx-welcome.tsx 2>/dev/null
rm -f apps/web/src/app/app.module.css 2>/dev/null
```

- [ ] **Step 5: Verify app chạy được**

```bash
npx nx serve web
```

Expected: Vite dev server chạy, truy cập `http://localhost:4200` thấy trang "Meli — Welcome to Meli." với MUI styling. Truy cập `/about` thấy trang About.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: wire up providers (MUI, React Query, TanStack Router)"
```

---

### Task 7: Configure ESLint & Prettier

**Files:**
- Modify: root ESLint config (generated by Nx)
- Create: `.prettierrc`

- [ ] **Step 1: Install Prettier + ESLint integration**

```bash
pnpm add -E -D prettier eslint-config-prettier
```

- [ ] **Step 2: Tạo .prettierrc**

Tạo file `.prettierrc` tại root:

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 80,
  "tabWidth": 2
}
```

- [ ] **Step 3: Tạo .prettierignore**

Tạo file `.prettierignore` tại root:

```
dist
node_modules
.nx
coverage
```

- [ ] **Step 4: Thêm prettier vào ESLint config**

Mở file ESLint config root (eslint.config.js hoặc .eslintrc.json — tùy Nx generate) và thêm `eslint-config-prettier` vào cuối extends array để tắt các rules xung đột:

Nếu flat config (`eslint.config.js`), thêm:

```javascript
const prettier = require('eslint-config-prettier');

// ... existing config
module.exports = [
  // ... existing entries
  prettier,
];
```

Nếu `.eslintrc.json`, thêm `"prettier"` vào cuối `"extends"` array.

- [ ] **Step 5: Verify lint chạy được**

```bash
npx nx lint web
```

Expected: Không có lỗi (hoặc chỉ warnings nhẹ).

- [ ] **Step 6: Verify Prettier format**

```bash
npx prettier --check "apps/web/src/**/*.{ts,tsx}"
```

Expected: Tất cả files đã đúng format, hoặc chạy `npx prettier --write` để fix.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "chore: configure ESLint with Prettier integration"
```

---

### Task 8: Configure Commitlint, Husky & lint-staged

**Files:**
- Create: `commitlint.config.ts`, `.husky/pre-commit`, `.husky/commit-msg`
- Modify: `package.json` (thêm lint-staged config)

- [ ] **Step 1: Install commitlint**

```bash
pnpm add -E -D @commitlint/cli @commitlint/config-conventional
```

- [ ] **Step 2: Tạo commitlint config**

Tạo `commitlint.config.ts` tại root:

```typescript
export default {
  extends: ['@commitlint/config-conventional'],
};
```

- [ ] **Step 3: Install Husky và lint-staged**

```bash
pnpm add -E -D husky lint-staged
```

- [ ] **Step 4: Init Husky**

```bash
npx husky init
```

Expected: Thư mục `.husky/` được tạo với file `pre-commit` mặc định.

- [ ] **Step 5: Tạo pre-commit hook**

Ghi đè file `.husky/pre-commit`:

```bash
npx lint-staged
```

- [ ] **Step 6: Tạo commit-msg hook**

Tạo file `.husky/commit-msg`:

```bash
npx --no -- commitlint --edit $1
```

- [ ] **Step 7: Thêm lint-staged config vào package.json**

Thêm vào `package.json`:

```json
{
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md,css}": ["prettier --write"]
  }
}
```

- [ ] **Step 8: Verify commitlint hoạt động**

```bash
echo "bad message" | npx commitlint
```

Expected: FAIL — "subject may not be empty", "type may not be empty".

```bash
echo "feat: test message" | npx commitlint
```

Expected: PASS — không có lỗi.

- [ ] **Step 9: Verify pre-commit hook**

```bash
git add -A
git commit -m "chore: configure commitlint, husky, and lint-staged"
```

Expected: lint-staged chạy (eslint + prettier) trên staged files, commitlint validate message, commit thành công.

---

### Task 9: Final Cleanup & Verification

**Files:**
- Possibly modify: various files for cleanup

- [ ] **Step 1: Verify build**

```bash
npx nx build web
```

Expected: Build thành công, output tại `dist/apps/web/`.

- [ ] **Step 2: Verify lint toàn bộ workspace**

```bash
npx nx run-many --target=lint --all
```

Expected: Tất cả projects lint pass.

- [ ] **Step 3: Verify dev server hoạt động đầy đủ**

```bash
npx nx serve web
```

Kiểm tra:
- `http://localhost:4200` — Trang home hiển thị "Meli" với MUI styling
- `http://localhost:4200/about` — Trang about hiển thị
- TanStack Router devtools hiện ở góc phải dưới
- React Query devtools hiện (nút nổi)
- Không có lỗi trong console

- [ ] **Step 4: Verify Nx dependency graph**

```bash
npx nx graph
```

Expected: Mở browser hiển thị graph với app `web` và 4 libs.

- [ ] **Step 5: Commit nếu có thay đổi cleanup**

```bash
git add -A
git commit -m "chore: final cleanup and verification" --allow-empty
```
