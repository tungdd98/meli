# Auth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm màn hình đăng nhập email/password và bảo vệ toàn bộ route (trừ `/login`) bằng Supabase Auth.

**Architecture:** Auth state được quản lý bởi Zustand store (`useAuthStore`). `providers.tsx` khởi tạo session khi app mount và block render router cho đến khi session được xác định (tránh race condition). TanStack Router dùng `beforeLoad` trên pathless layout route `_auth.tsx` để redirect về `/login` nếu không có session.

**Tech Stack:** Supabase JS (`@meli/api`), Zustand, TanStack Router (file-based), React Hook Form, Zod, `@hookform/resolvers/zod`, MUI v6.

---

## File Map

| File                                  | Action      | Trách nhiệm                                                                  |
| ------------------------------------- | ----------- | ---------------------------------------------------------------------------- |
| `apps/web/src/stores/auth.store.ts`   | **Tạo mới** | Auth state: session, user, isLoading; actions: signIn, signOut, \_setSession |
| `apps/web/src/app/providers.tsx`      | **Sửa**     | Thêm `AuthInitializer` để restore session và subscribe `onAuthStateChange`   |
| `apps/web/src/routes/_auth.tsx`       | **Tạo mới** | Pathless layout route với `beforeLoad` guard                                 |
| `apps/web/src/routes/_auth/index.tsx` | **Tạo mới** | Trang chủ `/` (nội dung từ `routes/index.tsx`)                               |
| `apps/web/src/routes/_auth/about.tsx` | **Tạo mới** | Trang `/about` (nội dung từ `routes/about.tsx`)                              |
| `apps/web/src/routes/login.tsx`       | **Tạo mới** | Trang login public với form RHF + Zod                                        |
| `apps/web/src/routes/index.tsx`       | **Xóa**     | Thay bằng `_auth/index.tsx`                                                  |
| `apps/web/src/routes/about.tsx`       | **Xóa**     | Thay bằng `_auth/about.tsx`                                                  |

---

## Task 1: Auth Store

**Files:**

- Tạo: `apps/web/src/stores/auth.store.ts`

- [ ] **Tạo file auth store**

```ts
// apps/web/src/stores/auth.store.ts
import { create } from 'zustand';
import { supabase } from '@meli/api';
import type { Session, User } from '@supabase/supabase-js';

interface AuthState {
  session: Session | null;
  user: User | null;
  isLoading: boolean;
  _setSession: (session: Session | null) => void;
  signIn: (email: string, password: string) => Promise<void>;
  signOut: () => Promise<void>;
}

export const useAuthStore = create<AuthState>((set) => ({
  session: null,
  user: null,
  isLoading: false,
  _setSession: (session) => set({ session, user: session?.user ?? null }),
  signIn: async (email, password) => {
    set({ isLoading: true });
    try {
      const { data, error } = await supabase.auth.signInWithPassword({
        email,
        password,
      });
      if (error) throw error;
      // Set session trực tiếp để route guard có session ngay trước khi navigate
      set({ session: data.session, user: data.session?.user ?? null });
    } finally {
      set({ isLoading: false });
    }
  },
  signOut: async () => {
    await supabase.auth.signOut();
  },
}));
```

- [ ] **Typecheck**

```bash
pnpm nx typecheck web
```

Expected: no errors.

- [ ] **Commit**

```bash
git add apps/web/src/stores/auth.store.ts
git commit -m "feat(auth): add auth store with signIn/signOut actions"
```

---

## Task 2: Session Initialization trong Providers

**Files:**

- Sửa: `apps/web/src/app/providers.tsx`

- [ ] **Thay toàn bộ nội dung `providers.tsx`**

```tsx
// apps/web/src/app/providers.tsx
import { useState, useEffect } from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';
import { RouterProvider } from '@tanstack/react-router';
import { Box, CircularProgress, CssBaseline } from '@mui/material';
import { ThemeProvider } from '@mui/material/styles';
import { theme } from '@meli/ui';
import { supabase } from '@meli/api';
import { useAuthStore } from '../stores/auth.store';
import { router } from './router';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,
      retry: 1,
    },
  },
});

function AuthInitializer({ children }: { children: React.ReactNode }) {
  const [authReady, setAuthReady] = useState(false);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      useAuthStore.getState()._setSession(session);
      setAuthReady(true);
    });

    const {
      data: { subscription },
    } = supabase.auth.onAuthStateChange((_, session) => {
      useAuthStore.getState()._setSession(session);
    });

    return () => subscription.unsubscribe();
  }, []);

  if (!authReady) {
    return (
      <Box
        sx={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          height: '100dvh',
        }}
      >
        <CircularProgress />
      </Box>
    );
  }

  return <>{children}</>;
}

export function Providers() {
  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />
      <QueryClientProvider client={queryClient}>
        <AuthInitializer>
          <RouterProvider router={router} />
        </AuthInitializer>
        <ReactQueryDevtools initialIsOpen={false} />
      </QueryClientProvider>
    </ThemeProvider>
  );
}
```

- [ ] **Typecheck**

```bash
pnpm nx typecheck web
```

Expected: no errors.

- [ ] **Commit**

```bash
git add apps/web/src/app/providers.tsx
git commit -m "feat(auth): initialize session in providers before rendering router"
```

---

## Task 3: Protected Layout Route

**Files:**

- Tạo: `apps/web/src/routes/_auth.tsx`

- [ ] **Tạo file `_auth.tsx`**

```tsx
// apps/web/src/routes/_auth.tsx
import { createFileRoute, redirect, Outlet } from '@tanstack/react-router';
import { useAuthStore } from '../stores/auth.store';

export const Route = createFileRoute('/_auth')({
  beforeLoad: () => {
    const { session } = useAuthStore.getState();
    if (!session) throw redirect({ to: '/login' });
  },
  component: () => <Outlet />,
});
```

- [ ] **Typecheck**

```bash
pnpm nx typecheck web
```

Expected: no errors (route tree sẽ được regenerate khi chạy dev server ở Task 4).

- [ ] **Commit**

```bash
git add apps/web/src/routes/_auth.tsx
git commit -m "feat(auth): add protected layout route with beforeLoad guard"
```

---

## Task 4: Dời Existing Routes vào `_auth/`

**Files:**

- Tạo: `apps/web/src/routes/_auth/index.tsx`
- Tạo: `apps/web/src/routes/_auth/about.tsx`
- Xóa: `apps/web/src/routes/index.tsx`
- Xóa: `apps/web/src/routes/about.tsx`

- [ ] **Tạo `_auth/index.tsx`**

```tsx
// apps/web/src/routes/_auth/index.tsx
import { createFileRoute } from '@tanstack/react-router';
import { Container, Typography } from '@mui/material';

export const Route = createFileRoute('/_auth/')({
  component: HomePage,
});

function HomePage() {
  return (
    <Container maxWidth="sm" sx={{ mt: 4 }}>
      <Typography variant="h3" gutterBottom>
        Meli
      </Typography>
      <Typography variant="body1">Welcome to Meli.</Typography>
    </Container>
  );
}
```

- [ ] **Tạo `_auth/about.tsx`**

```tsx
// apps/web/src/routes/_auth/about.tsx
import { createFileRoute } from '@tanstack/react-router';
import { Container, Typography } from '@mui/material';

export const Route = createFileRoute('/_auth/about')({
  component: AboutPage,
});

function AboutPage() {
  return (
    <Container maxWidth="sm" sx={{ mt: 4 }}>
      <Typography variant="h4" gutterBottom>
        About
      </Typography>
      <Typography variant="body1">About Meli.</Typography>
    </Container>
  );
}
```

- [ ] **Xóa các file cũ**

```bash
rm apps/web/src/routes/index.tsx
rm apps/web/src/routes/about.tsx
```

- [ ] **Chạy dev server để regenerate route tree, rồi tắt ngay**

```bash
pnpm nx dev web
# Chờ thấy "Local: http://localhost:4200" rồi Ctrl+C
```

Route tree tại `apps/web/src/app/routeTree.gen.ts` sẽ được cập nhật tự động.

- [ ] **Typecheck**

```bash
pnpm nx typecheck web
```

Expected: no errors.

- [ ] **Commit**

```bash
git add apps/web/src/routes/_auth/ apps/web/src/app/routeTree.gen.ts
git rm apps/web/src/routes/index.tsx apps/web/src/routes/about.tsx
git commit -m "feat(auth): move routes into protected _auth layout"
```

---

## Task 5: Login Page

**Files:**

- Tạo: `apps/web/src/routes/login.tsx`

- [ ] **Tạo file `login.tsx`**

**UI reference:** `docs/features/auth/2026-05-24-auth-login.pen` — dùng file thiết kế này làm chuẩn cho toàn bộ layout, màu sắc, spacing, typography. Code dưới đây chỉ định nghĩa logic, imports, và skeleton render.

```tsx
// apps/web/src/routes/login.tsx
import { useState } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { createFileRoute, redirect, useNavigate } from '@tanstack/react-router';
import {
  Alert,
  Box,
  Button,
  CircularProgress,
  IconButton,
  InputAdornment,
  Paper,
  Stack,
  Typography,
} from '@mui/material';
import { VisibilityOffRounded, VisibilityRounded } from '@mui/icons-material';
import { FormTextField } from '@meli/ui';
import { useAuthStore } from '../stores/auth.store';

const loginSchema = z.object({
  email: z.string().email('Email không hợp lệ'),
  password: z.string().min(6, 'Mật khẩu tối thiểu 6 ký tự'),
});

type LoginFormValues = z.infer<typeof loginSchema>;

export const Route = createFileRoute('/login')({
  beforeLoad: () => {
    const { session } = useAuthStore.getState();
    if (session) throw redirect({ to: '/' });
  },
  component: LoginPage,
});

function LoginPage() {
  const [showPassword, setShowPassword] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const navigate = useNavigate();
  const { signIn, isLoading } = useAuthStore();

  const { control, handleSubmit } = useForm<LoginFormValues>({
    resolver: zodResolver(loginSchema),
    defaultValues: { email: '', password: '' },
  });

  const onSubmit = async (values: LoginFormValues) => {
    setError(null);
    try {
      await signIn(values.email, values.password);
      navigate({ to: '/' });
    } catch (err) {
      const message =
        err instanceof Error &&
        err.message.includes('Invalid login credentials')
          ? 'Email hoặc mật khẩu không đúng'
          : 'Không thể kết nối. Vui lòng thử lại';
      setError(message);
    }
  };

  // Implement JSX theo docs/features/auth/2026-05-24-auth-login.pen
  // Các thành phần cần render (theo thứ tự trong file .pen):
  //   - Baby Image Logo (124×124, src: assets/baby.png) + brand name "Meli"
  //   - Heading "Đăng nhập" + subtitle
  //   - FormTextField name="email" type="email" autoComplete="email"
  //   - FormTextField name="password" type={showPassword ? 'text' : 'password'}
  //       với endAdornment: IconButton toggle VisibilityRounded / VisibilityOffRounded
  //   - {error && <Alert severity="error">{error}</Alert>}
  //   - Button type="submit" variant="contained" disabled={isLoading}
  //       nội dung: isLoading ? <CircularProgress size={20} color="inherit" /> : 'Đăng nhập'
}
```

- [ ] **Chạy dev server để regenerate route tree**

```bash
pnpm nx dev web
# Chờ thấy "Local: http://localhost:4200" rồi Ctrl+C
```

- [ ] **Typecheck**

```bash
pnpm nx typecheck web
```

Expected: no errors.

- [ ] **Commit**

```bash
git add apps/web/src/routes/login.tsx apps/web/src/app/routeTree.gen.ts
git commit -m "feat(auth): add login page with email/password form"
```

---

## Task 6: Smoke Test

Chạy dev server và kiểm tra tất cả các luồng.

- [ ] **Khởi động dev server**

```bash
pnpm nx dev web
```

- [ ] **Test redirect unauthenticated → /login**

Mở `http://localhost:4200` trong browser khi chưa đăng nhập.
Expected: tự động redirect sang `http://localhost:4200/login`.

- [ ] **Test validation inline**

Để trống email và password, nhấn "Đăng nhập".
Expected: error message hiển thị dưới từng field ("Email không hợp lệ", "Mật khẩu tối thiểu 6 ký tự").

- [ ] **Test wrong credentials**

Nhập email/password sai, nhấn "Đăng nhập".
Expected: Alert "Email hoặc mật khẩu không đúng" hiển thị phía trên nút submit.

- [ ] **Test login thành công**

Nhập email/password đúng (tạo account trên Supabase dashboard trước), nhấn "Đăng nhập".
Expected: redirect về `http://localhost:4200/`.

- [ ] **Test /login khi đã đăng nhập**

Trong lúc đang đăng nhập, truy cập `http://localhost:4200/login`.
Expected: tự redirect về `/`.

- [ ] **Test password show/hide**

Nhấn icon mắt trong field Mật khẩu.
Expected: text hiển thị/ẩn đúng.
